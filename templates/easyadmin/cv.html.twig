{% extends '@EasyAdmin/page/content.html.twig' %}

{% block main %}
<div class="px-3">
    <div class="row bg-dark">
        <div class="col-6 p-0">
            {{ form_start(form) }}
            {{ form_widget(form.cv) }}
            {{ form_end(form) }}

{#            <div class="text-right mt-2">#}
{#                <button class="btn btn-success cv-save" data-url="{{ url('admin_cv_preview',{preview:'cv'}) }}">#}
{#                    Zapisz#}
{#                </button>#}
{#                <button class="btn btn-warning cv-revert" data-url="{{ url('admin_cv_revert') }}">#}
{#                    Przywróć#}
{#                </button>#}
{#                <button class="btn btn-primary cv-save"#}
{#                        data-url="{{ url('admin_cv_preview',{preview:'cv_example'}) }}">#}
{#                    Podgląd#}
{#                </button>#}
{#            </div>#}
        </div>

        <div class="col-6 p-0">
            <embed src="{{ asset('upload/cv_example.pdf') }}" type="application/pdf" width="100%" height="1200px"
                   id="cv"/>
        </div>
    </div>
</div>
{% endblock %}
